<h1>Listing show_aliases</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Show</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @show_aliases.each do |show_alias| %>
  <tr>
    <td><%= show_alias.name %></td>
    <td><%= show_alias.show %></td>
    <td><%= link_to 'Show', show_alias %></td>
    <td><%= link_to 'Edit', edit_show_alias_path(show_alias) %></td>
    <td><%= link_to 'Destroy', show_alias, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Show alias', new_show_alias_path %>
