<%= form_for(@tagging) do |f| %>
  <% if @tagging.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tagging.errors.count, "error") %> prohibited this tagging from being saved:</h2>

      <ul>
      <% @tagging.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">  
	<% if @show.nil? %>
    <%= f.hidden_field :show_id, :value => Show.first.id %>
	<% else %>
    <%= f.hidden_field :show_id, :value => @show.id %>
	<% end %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br />
    <%= f.text_field :tagtext %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
